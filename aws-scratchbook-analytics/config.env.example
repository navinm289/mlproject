# Copy to config.env and fill in (optional local reference for REPLACE_* values)
ACCOUNT_ID=
REGION=us-east-1
LAKE_BUCKET=
HMS_NAMESPACE=metastore
HMS_THRIFT_URI=thrift://hive-metastore.metastore.svc.cluster.local:9083
EFS_FILE_SYSTEM_ID=
ECR_REPO=
HUB_HOSTNAME=
OIDC_ISSUER=
PILOT_GROUP=risk-analysts
